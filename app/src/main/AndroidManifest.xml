<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.clover.permission.ACCESS_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Scanmonitor"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ScanMonitorService"
            android:enabled="true"
            android:exported="false"/>


        <receiver android:name=".BarcodeReceiver" android:exported="true">
            <intent-filter>
                <!-- Zebra DataWedge -->
                <action android:name="com.example.scanmonitor.SCAN" />
                <!-- Clover built-in scanner -->
                <action android:name="com.clover.intent.action.BARCODE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>